package org.mp.naumann.algorithms.benchmark.speed;

import org.junit.Test;

public class SpeedTest {

    @Test
    public void testSpeed(){
       /* Speed s = new Speed(System.out::println);
        s.start("Beginning with speed test");
        for(long i = 0; i < 1000000; i++){
            if(i % 1273 == 1)
                Speed.lap(new StringListSpeedInfo("i % 1273 == 1", String.valueOf(i)));
        }
        s.end(new SpeedInfo("End of speed test")); */
    }

}
